require "spec_helper"

describe Wor::Requests do
  it "has a version number" do
    expect(Wor::Requests::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
