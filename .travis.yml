language: ruby
rvm:
  - 2.3.3
  - 2.4.0
  - 2.5.1
  - ruby-head

install:
  - gem install bundler
  - bundle install --retry=3

script:
  - bundle exec rspec
  - bundle exec rubocop -R --format simple
  - bundle exec codeclimate-test-reporter

notifications:
  email: false